# Passo 30 - Ajustar a integração da interface (Slide 26)

O caminho feliz não basta. Vamos provocar uma falha de validação e fazer um ajuste pequeno para preservar os dados preenchidos e comunicar o erro.

Continue a sessão anterior. Selecione somente o bloco da resposta HTTP, abra o **Inline Chat** e execute o prompt. Revise antes de aceitar e ajuste manualmente conforme necessário.


Execute este prompt:

```text
Ajuste somente este bloco para preservar os dados preenchidos quando a API retornar erro de validação e exibir a mensagem recebida.

Não altere o contrato da API, não reorganize o componente e não modifique o caminho de sucesso.
```

> [!TIP]
> Se necessário, sincronize com `checkpoint/06-conectar-interface`.

Quando tiver concluído, comente `validado` nesta issue para receber o próximo passo.


